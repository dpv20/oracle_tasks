# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
.claude/
to-do.md

# PyInstaller (not used now but reserved)
build/
dist/
*.spec

# User-specific config (each user has their own in %APPDATA%)
config.json

# Logs
*.log
app.log

# Generated spools (live in %LOCALAPPDATA%\OracleTasksChile at runtime)
spools_CL_out/
spools_savings_out/
spools_out/

# Local SQLcl install (downloaded to %LOCALAPPDATA% at runtime)
sqlcl/
sqlcl-latest.zip

# Local temp folders
tmp*/
temp_savings/



# Local reference material (kept locally for context, not part of the app)
spools_CL/spools_files/
cuenta_prod_a_QA.txt
pantalla_*.png
tnsnames.ora

# Internal project docs (kept local, not for end users)
agent.md
implementation_plan.md
progress.md

# OS
Thumbs.db
Desktop.ini
.DS_Store
